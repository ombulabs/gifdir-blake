<p id="notice"><%= notice %></p>

<%= image_tag @image.uploaded_image.url %>

<% @image.tags.each do |tag|   %>
  <%= tag %>
<% end %>

<%= link_to 'Edit', edit_image_path(@image) %> |
<%= link_to 'Back', images_path %>
